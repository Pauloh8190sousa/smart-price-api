spring:
  application:
    name: smart-price-api

  profiles:
    active: dev

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

app:
  version: '@project.version@'

resend:
  api-key: ${RESEND_API_KEY}